<div class="container index-post-container">
  <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3">
    <% @posts.each do |post| %>
      <% if post.image && post.image.url %>
        <div class="col">
          <%= link_to (image_tag post.image.thumb.url, class: "index-post" ), post_path(post.id)
          %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
